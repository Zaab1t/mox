import os
import sys
import logging
logging.basicConfig(stream=sys.stderr)

if os.getenv('NO_VENV') != '1':
    activate_this = '{{ENVDIR}}/bin/activate_this.py'
    execfile(activate_this, dict(__file__=activate_this))

    sys.path.append('/srv/mox/oio_rest')

sys.path.append('{{DIR}}')

import oio_rest.app as app


application = app.app

app.setup_api()
